import type{Metadata}from"next";import{notFound}from"next/navigation";import{copy,type Lang}from"../../../content";import{Footer,Header,Statement}from"../../../components";
export const dynamicParams=false;export function generateStaticParams(){return[{lang:"uk"},{lang:"ru"}]}
export async function generateMetadata({params}:{params:Promise<{lang:string}>}):Promise<Metadata>{const{lang}=await params;if(!(lang in copy))return{};const c=copy[lang as Lang],title=`${c.statement} — Симона Вилар`,url=`https://simonavilar.com/${lang}/news/official-statement`;return{title,description:c.paras[0],alternates:{canonical:url,languages:{uk:"/uk/news/official-statement",ru:"/ru/news/official-statement"}},openGraph:{title,description:c.paras[0],siteName:"Симона Вилар",locale:lang==="uk"?"uk_UA":"ru_RU",type:"article",url}}}
export default async function Page({params}:{params:Promise<{lang:string}>}){const{lang:r}=await params;if(!(r in copy))notFound();const lang=r as Lang,c=copy[lang];return <><Header lang={lang} detail/><main className="article shell"><a className="back" href={`/${lang}/news`}>← {c.back}</a><Statement lang={lang}/></main><Footer lang={lang} detail/></>}
